{
  "$jsonSchema": {
    "bsonType": "object",
    "properties": {
      "end_time": {
        "bsonType": "date",
        "description": "must be `date` & is required"
      },
      "is_active": {
        "bsonType": "bool",
        "description": "must be `bool` & is required"
      },
      "is_break": {
        "bsonType": "bool",
        "description": "must be `bool` & is required"
      },
      "is_focus": {
        "bsonType": "bool",
        "description": "must be `bool` & is required"
      },
      "start_time": {
        "bsonType": "date",
        "description": "must be `date` & is required"
      },
      "user_id": {
        "bsonType": "int",
        "description": "must be `int` & is required",
        "minimum": 0
      },
      "version": {
        "bsonType": "string",
        "description": "must be `string` & is required"
      }
    },
    "required": [
      "user_id",
      "start_time",
      "end_time",
      "is_active",
      "is_focus",
      "is_break"
    ]
  }
}
